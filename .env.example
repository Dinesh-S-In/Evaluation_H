# Live OpenAI judging (optional on Vercel — set in Project Settings → Environment Variables)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# When true, forces mock scoring even if a key is present (optional)
MOCK_EVALUATION=false
